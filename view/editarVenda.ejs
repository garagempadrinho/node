<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Venda</title>
</head>

<body>
    <h2>Editar Venda</h2>
    <form action="/vendas/editar/<%= venda.id_venda %>" method="POST">
        <label>Valor da Venda:</label>
        <input type="number" step="0.01" name="valor_venda" value="<%= venda.valor_venda %>" required><br>

        <label>Desconto:</label>
        <input type="number" step="0.01" name="desconto" value="<%= venda.desconto %>" required><br>

        <label>Cliente:</label>
        <select name="id_cliente">
            <% clientes.forEach(cliente=> { %>
                <option value="<%= cliente.id_cliente %>" <%=cliente.id_cliente===venda.id_cliente ? "selected" : "" %>>
                    <%= cliente.nome %>
                </option>
                <% }) %>
        </select><br>

        <button type="submit">Salvar</button>
    </form>

</body>

</html>
